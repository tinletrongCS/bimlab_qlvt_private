# QLVT repo — caller của reusable CI platform (STEP-4 4-e). Consumer CT-5(+CT-4), không provide openapi.
name: ci
on:
  pull_request:
  push: { branches: [main] }
jobs:
  ci:
    uses: zafkiel-ltb/platform_bimlab/.github/workflows/reusable-ci.yml@v1
    with:
      java_services: '[{"name":"asset","dir":"asset-service"}]'
      openapi_files: ""        # QLVT chỉ consume → không lint openapi provider
      python_dir: ""
